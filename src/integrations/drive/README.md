This folder implements the SintraPrime Drive Tooling Pack.

Primary entrypoints:
- `driveEnsurePath` (idempotent folder creation)
- `driveApplyTemplate` (batch ensurePath for known templates)
- `driveAuthTest` (capability registry update)
