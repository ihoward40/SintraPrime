# C:\SintraPrime (Template)

This folder represents a suggested Windows-local root at `C:\SintraPrime`.

## Directories

- `bin/` — launchers/binaries (placeholders only).
- `VERIFY/` — public keys and verification materials.
- `STATE/` — local state/authorization artifacts (if used).
- `REPORTS/` — generated outputs produced locally.

No private keys, credentials, or network configuration are implied by this layout.
