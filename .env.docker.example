# Docker Compose env (copy to .env.docker and edit)
# NOTE: Keep real secrets out of git.

# MySQL (matches mysql/init/01-init.sql defaults)
MYSQL_ROOT_PASSWORD=sintraprime_root_password
MYSQL_DATABASE=sintraprime
MYSQL_USER=sintraprime_user
MYSQL_PASSWORD=sintraprime_password

# Airlock required secrets
MANUS_SHARED_SECRET=change-me-manus-shared-secret
AIRLOCK_SHARED_SECRET=change-me-airlock-shared-secret
MAKE_WEBHOOK_URL=http://127.0.0.1:8999/replace-me

# Webapp required secret
JWT_SECRET=change-me-local-jwt-secret

# Optional
# OPENAI_API_KEY=
