/** @type {import('next').NextConfig} */
const nextConfig = {
  // Intentionally minimal. This console runs as a separate dev server
  // and talks to the SintraPrime router via NEXT_PUBLIC_SINTRA_BASE_URL.
};

export default nextConfig;
