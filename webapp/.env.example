# Webapp env template
#
# Keep secrets in webapp/.env.local (do not commit). This template is safe to commit.

# Webapp server port (keep :3000 free for Airlock)
PORT=3002

# Hub Registry - Connect to local services
SINTRA_HUB_URL=http://127.0.0.1

# Optional explicit per-service URLs
SINTRA_AIRLOCK_URL=http://127.0.0.1:3000
SINTRA_BRAIN_URL=http://127.0.0.1:8011
SINTRA_API_URL=http://127.0.0.1:8000

# App identity / auth
VITE_APP_ID=sintraprime_local
OAUTH_SERVER_URL=https://api.manus.im

# Secrets (set these in webapp/.env.local)
# DATABASE_URL=
# JWT_SECRET=
